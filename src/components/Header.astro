<header class="header">
  <div class="container nav">
    <a href="/" class="brand">
      <span class="logo">HECL</span>
      <span class="subtitle">Hydrology &amp; EcoClimate Lab</span>
    </a>

    <nav aria-label="Primary navigation">
      <a href="/#research">Research</a>
      <a href="/people">People</a>
      <a href="/publications">Publications</a>
      <a href="/projects">Projects</a>
      <a href="/news">News</a>
      <a href="/join">Join Us</a>
    </nav>
  </div>
</header>

<style>
.header{
  position:sticky;
  top:0;
  z-index:100;
  background:rgba(255,255,255,.92);
  backdrop-filter:blur(12px);
  border-bottom:1px solid var(--line);
}

.nav{
  display:flex;
  justify-content:space-between;
  align-items:center;
  min-height:82px;
}

.brand{
  display:flex;
  flex-direction:column;
  text-decoration:none;
  line-height:1;
}

.logo{
  font-size:1.9rem;
  font-weight:800;
  letter-spacing:.08em;
  color:var(--ink);
}

.subtitle{
  margin-top:6px;
  font-size:.78rem;
  font-weight:500;
  letter-spacing:.12em;
  text-transform:uppercase;
  color:var(--muted);
}

nav{
  display:flex;
  gap:32px;
}

nav a{
  font-size:.95rem;
  font-weight:600;
  color:var(--ink);
  text-decoration:none;
  transition:color .2s ease;
}

nav a:hover{
  color:var(--accent);
}

@media (max-width:900px){

  .nav{
    min-height:72px;
  }

  nav{
    display:none;
  }

  .logo{
    font-size:1.65rem;
  }

  .subtitle{
    font-size:.72rem;
  }

}
</style>
